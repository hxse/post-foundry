## Task ID `20260622A`

### Summary
* 路径：`doc/tasks/snapshots/20260622A post-foundry foundation/20260622A/`
* 状态：文档草案，待人工 Document Gate。
* 范围：冻结 PostFoundry 第一主任务的产品愿景、项目基础、技术栈、多账号隔离、API 边界、任务拆分和验证口径。
* 相关 active spec / guide：无；项目尚未进入 Close Gate，当前真值在本 task 快照中。
* 摘要：PostFoundry 的长期目标是让 Codex 中的 AI 辅助运营自有 X 账号：统计热点、查找资料、生成草稿、排程发帖、读取表现并复盘经验。项目使用 TypeScript、Bun、克制使用的 SvelteKit、SQLite、Drizzle、Zod、Vitest 和 `just` 建本地轻量运营后台。第三方 X 数据 API 只读公开数据，X 官方 API 只做发帖等允许的账号动作；禁止水军、刷量、反检测浏览器、网页登录自动化、cookie 代理、无人自动隐藏回复，以及把在线或可能计费的 API 调用做成自动测试。

### Relations
* `independent`。

## Task ID `20260622A.001`

### Summary
* 路径：`doc/tasks/snapshots/20260622A post-foundry foundation/20260622A.001 api connectivity harness/`
* 状态：已收口，Close Gate 通过。
* 范围：冻结并实现第一子任务的 API 连通性测试框架、离线 fixture 测试、在线 debug 入口、真实发帖保护开关、第三方读回、secrets 权限、错误语义和验收标准。
* 相关 active spec / guide：`04_review/01_close_gate_review.md`。
* 摘要：同一个 API 子任务内保留两个 `just` 入口：`just test-offline-api` 是正式离线 Gate，不访问网络；`just debug-online-api-smoke` 是用户明确要求后的手动在线诊断入口，访问 TwitterAPI.io 和 X 官方 API，可能产生费用，不能进入默认测试、CI 或 agent 自主 Close Gate，真实发帖必须显式打开硬开关。

### Relations
* `splits-from` `20260622A`：从第一主任务中拆出的 API 连通性验证子任务。

## Task ID `20260622A.002`

### Summary
* 路径：`doc/tasks/snapshots/20260622A post-foundry foundation/20260622A.002 account registry and config isolation/`
* 状态：已收口，Close Gate 通过。
* 范围：冻结并实现多账号注册表、非敏感账号配置 schema、`account_uuid` / `account_key` / X identity 分层、可信 resolver、配置版本快照、账号 key 重命名审计和账号级预算/主题/风格/数据源隔离。
* 相关 active spec / guide：`04_review/01_close_gate_review.md`。
* 摘要：账号配置以 `account_uuid` 作为内部不可变归属真值，`account_key` 只是可重命名别名。普通配置只保存非敏感字段和 token 状态，不保存 token 本体；离线测试证明重复账号、敏感字段、key/uuid 不一致和重命名归属风险都会被拦截。

### Relations
* `splits-from` `20260622A`：从第一主任务中拆出的账号注册表和配置隔离子任务。
* `follows` `20260622A.001`：建立在 API 边界、secrets 权限和离线/在线验证分层已收口之后。

## Task ID `20260622A.003`

### Summary
* 路径：`doc/tasks/snapshots/20260622A post-foundry foundation/20260622A.003 runtime skeleton and storage baseline/`
* 状态：已收口，Close Gate 通过。
* 范围：冻结并实现本地运行骨架、SQLite 文件路径配置、Drizzle schema、runtime migration、repo 层、job 状态、API audit、runtime health 和薄 SvelteKit 状态页。
* 相关 active spec / guide：`04_review/01_close_gate_review.md`。
* 摘要：`.003` 把 `.001` API contract 和 `.002` 账号 registry/config contract 接到本地 SQLite runtime 中。默认数据库文件为 ignored 的 `data/post-foundry.sqlite`，账号相关 runtime 数据全部按 `account_uuid` 归属，测试只使用离线 SQLite，不访问真实外部服务。

### Relations
* `splits-from` `20260622A`：从第一主任务中拆出的 runtime skeleton 和 storage baseline 子任务。
* `follows` `20260622A.002`：建立在账号注册表和配置隔离 contract 已收口之后。

## Task ID `20260622A.004`

### Summary
* 路径：`doc/tasks/snapshots/20260622A post-foundry foundation/20260622A.004 audit event ledger baseline/`
* 状态：已收口，Close Gate 通过。
* 范围：冻结并实现 AI 自动化运营的审计总账基础，包括 AI run、decision、action、evidence、human review 和通用 audit event；所有记录按 `account_uuid` 归属，并拒绝跨账号引用。
* 相关 active spec / guide：`04_review/01_close_gate_review.md`。
* 摘要：`.004` 把“可沉淀、可复盘、可审计”落成 runtime 数据结构和 repo API。普通无链接帖后续可以走自动化策略，但 AI 的输入、证据、判断和动作都必须写入 ledger；带链接帖后续由 Telegram human gate 审批，审批结果也进入 ledger。本子任务只做本地离线审计底座，不执行在线 API、真实发帖或 Telegram 发送。

### Relations
* `splits-from` `20260622A`：从第一主任务中拆出的 audit/event ledger 子任务。
* `follows` `20260622A.003`：建立在 SQLite runtime baseline 已收口之后。

## Task ID `20260622A.005`

### Summary
* 路径：`doc/tasks/snapshots/20260622A post-foundry foundation/20260622A.005 automation policy engine v0/`
* 状态：已收口，Close Gate 通过。
* 范围：冻结并实现自动化策略引擎 v0，对候选帖输出 `auto_post` / `human_review` / `reject` / `defer`，并提供写入 `.004` ledger 的 helper。
* 相关 active spec / guide：`04_review/01_close_gate_review.md`。
* 摘要：`.005` 把“无链接普通帖可自动发、带链接帖进 Telegram human gate、账号主题/预算/频率/风格边界不可绕过”落成离线 policy decision。它不执行真实发帖、不发送 Telegram、不访问在线 API；后续 executor 和 bot 必须消费 policy result 并继续写 ledger。

### Relations
* `splits-from` `20260622A`：从第一主任务中拆出的 automation policy 子任务。
* `follows` `20260622A.004`：建立在 audit/event ledger 已收口之后。

## Task ID `20260622A.006`

### Summary
* 路径：`doc/tasks/snapshots/20260622A post-foundry foundation/20260622A.006 telegram notification connectivity harness/`
* 状态：已收口，Close Gate 通过。
* 范围：冻结并实现 Telegram 通知连通性 harness，包括 Telegram secrets schema、官方 Bot API adapter、dry-run 默认的在线 debug 入口、通知文案保护和离线测试。
* 相关 active spec / guide：`04_review/01_close_gate_review.md`。
* 摘要：`.006` 只做通知，不做审批按钮、webhook、long polling 或回调处理。后续需要人工处理的候选帖可以先通知到用户自己的 Telegram 频道；真实发送必须由用户填好本地 secrets 后手动运行 `debug-online-telegram --send`。

### Relations
* `splits-from` `20260622A`：从第一主任务中拆出的 Telegram notification connectivity 子任务。
* `follows` `20260622A.005`：建立在 automation policy 已能输出 human review / notification route 之后。

## Task ID `20260622A.007`

### Summary
* 路径：`doc/tasks/snapshots/20260622A post-foundry foundation/20260622A.007 manual notification workflow/`
* 状态：已收口，Close Gate 通过。
* 范围：冻结并实现人工处理通知流，把 `.005` policy 的 `human_review / telegram_human_gate` 结果转成 Telegram 通知 payload，并把发送成功/失败写入 `.004` ledger。
* 相关 active spec / guide：`04_review/01_close_gate_review.md`。
* 摘要：`.007` 明确通知规则：带链接或其他人工处理候选帖通知到 Telegram；`auto_post` 不通知，后续交给自动发帖 executor；`reject` 和 `defer` 默认只入账本不实时打扰。带链接帖通知用户处理，是因为 X 官方 API 发送带链接帖子成本更高。

### Relations
* `splits-from` `20260622A`：从第一主任务中拆出的 manual notification workflow 子任务。
* `follows` `20260622A.006`：建立在 Telegram notification connectivity 已收口之后。

## Task ID `20260622A.008`

### Summary
* 路径：`doc/tasks/snapshots/20260622A post-foundry foundation/20260622A.008 account prompt baseline/`
* 状态：已收口，Close Gate 通过。
* 范围：冻结并实现账号级初始提示加载 baseline。真实 prompt 只能放在 ignored 的 `secrets/accounts.local.json` 内联字段，或放在 ignored 的 `secrets/**/*.md` 文件中并由 secrets 引用；二者只能选一个。
* 相关 active spec / guide：`04_review/01_close_gate_review.md`。
* 摘要：`.008` 解决“AI 怎么知道每个账号该围绕什么主题和语气运营”的入口问题。非敏感账号配置继续保存结构化主题、预算和风格边界；给 AI 看的自然语言初始提示只从本地 secrets 加载，支持 `initial_prompt` 或 `initial_prompt_path`，文件路径必须在 `secrets/` 下并以 `.md` 结尾，例如 `secrets/prompts/zh-tech.md`。加载结果包含 prompt hash，后续 AI run 可写入 ledger 做复盘和审计。

### Relations
* `splits-from` `20260622A`：从第一主任务中拆出的 account prompt baseline 子任务。
* `follows` `20260622A.007`：建立在 policy/manual notification 工作流已能按账号隔离之后。

## Task ID `20260622A.009`

### Summary
* 路径：`doc/tasks/snapshots/20260622A post-foundry foundation/20260622A.009 ai posting pipeline baseline/`
* 状态：已收口，Close Gate 通过。
* 范围：冻结并实现 AI 发帖草稿 pipeline 的离线 baseline，包括 draft input package、AI draft output schema、自然发帖文本 gate、近期重复检测、evidence 引用校验和 ledger 写入。
* 相关 active spec / guide：`04_review/01_close_gate_review.md`。
* 摘要：`.009` 把“近期热点 + 高赞风格学习 + 重新查资料 + 近期发帖去重 + 自然发帖文本”落成可审计的数据 contract。内部落盘可以结构化，外部 `post_text` 必须是自然 plain text，不能是 Markdown、列表、标题、debug 或 task 测试文案；短帖后续可自动发，长帖进入 Telegram human gate。本任务不接在线 LLM、不抓真实热点、不发帖，只提供后续真实 AI 运行必须遵守的离线骨架。

### Relations
* `splits-from` `20260622A`：从第一主任务中拆出的 AI posting pipeline baseline 子任务。
* `follows` `20260622A.008`：建立在账号级初始 prompt 已可加载并可用 hash 审计之后。

## Task ID `20260622A.010`

### Summary
* 路径：`doc/tasks/snapshots/20260622A post-foundry foundation/20260622A.010 recent context and source ingestion baseline/`
* 状态：已收口，Close Gate 通过。
* 范围：冻结并实现近期上下文和资料 source ingestion 的离线 baseline，把近期发帖、公开 X 热点/高赞帖、网页/新闻资料和人工笔记统一成 `.009` 可消费的 source context，并写入 `.004` ledger。
* 相关 active spec / guide：`04_review/01_close_gate_review.md`。
* 摘要：`.010` 补齐 `.009` 上游输入：按账号隔离近期已发帖，按 topic 汇总、过滤、排序资料素材，保留 evidence refs 和 ingestion audit event。它不访问真实 X、新闻站点或在线 LLM；后续真实 adapter 只能产出本任务定义的 source material contract，再交给 `.009` 生成草稿。

### Relations
* `splits-from` `20260622A`：从第一主任务中拆出的 recent context/source ingestion baseline 子任务。
* `follows` `20260622A.009`：建立在 draft input package、自然发帖文本 gate 和 policy 接口已收口之后。

## Task ID `20260622A.011`

### Summary
* 路径：`doc/tasks/snapshots/20260622A post-foundry foundation/20260622A.011 adapter boundary and fixture baseline/`
* 状态：已收口，Close Gate 通过。
* 范围：冻结并实现真实数据 adapter 的离线边界，包括 TwitterAPI.io public X search 到 `SourceMaterialInput` 的转换、manual note fixture、web/news fixture 和 source adapter API audit contract。
* 相关 active spec / guide：`04_review/01_close_gate_review.md`。
* 摘要：`.011` 不接真实在线数据，只定义未来 adapter 必须遵守的输出边界。TwitterAPI.io adapter 只读公开 X 数据并转换为 `.010` source material；manual/web fixtures 不访问外部网络；所有 adapter run 必须能产生 `api_call_audit` 记录，后续在线入口才能审计费用、失败和调用量。

### Relations
* `splits-from` `20260622A`：从第一主任务中拆出的 adapter boundary/fixture baseline 子任务。
* `follows` `20260622A.010`：建立在 source context contract 已收口之后。

## Task ID `20260622A.012`

### Summary
* 路径：`doc/tasks/snapshots/20260622A post-foundry foundation/20260622A.012 topic radar and selection baseline/`
* 状态：已收口，Close Gate 通过。
* 范围：冻结并实现账号级选题雷达 baseline，把 `.011` 的 source materials 和近期已发帖整理成 ranked topic candidates，避开近期重复主题，输出 `.010` / `.009` 可消费的 `selectedTopic`，并写入 `.004` ledger。
* 相关 active spec / guide：`04_review/01_close_gate_review.md`。
* 摘要：`.012` 不接真实在线数据，不调用在线 LLM，只提供可回放、可审计的离线选题层。它按账号 include/exclude topic 过滤素材，对候选题打分，压制近期语义重复选题，并把 prompt hash、证据、候选题、discard reason 和最终选择写入 ledger。

### Relations
* `splits-from` `20260622A`：从第一主任务中拆出的 topic radar/selection baseline 子任务。
* `follows` `20260622A.011`：建立在 adapter boundary 和 source material contract 已收口之后。

## Task ID `20260622A.013`

### Summary
* 路径：`doc/tasks/snapshots/20260622A post-foundry foundation/20260622A.013 offline orchestration run baseline/`
* 状态：已收口，Close Gate 通过。
* 范围：冻结并实现单次离线 orchestration run，把 topic radar、source context、draft pipeline、draft gate、automation policy 和 manual notification workflow 串成同一个 `trace_id` 下的可审计闭环。
* 相关 active spec / guide：`04_review/01_close_gate_review.md`。
* 摘要：`.013` 不接真实在线服务，不调用 LLM、X official API、TwitterAPI.io 或真实 Telegram。它使用 source fixture、fake draft output 和 fake Telegram sender 验证：无链接短帖能落为 offline auto-post planned action；带链接帖能进入 fake Telegram human gate；格式化草稿会在 policy 前被 draft gate 拦住；所有关键步骤写入 ledger 并可按 trace 回放。

### Relations
* `splits-from` `20260622A`：从第一主任务中拆出的 offline orchestration run baseline 子任务。
* `follows` `20260622A.012`：建立在 topic radar 已能输出可审计 selected topic 之后。

## Task ID `20260622A.014`

### Summary
* 路径：`doc/tasks/snapshots/20260622A post-foundry foundation/20260622A.014 account memory and reflection baseline/`
* 状态：已收口，Close Gate 通过。
* 范围：冻结并实现账号级 memory/reflection baseline，从同账号 ledger trace 聚合近期选题、草稿 hash、policy outcome、final action、证据和复盘提示，并把 deterministic reflection 写回 ledger。
* 相关 active spec / guide：`04_review/01_close_gate_review.md`。
* 摘要：`.014` 把“可沉淀、可复盘”推进到跨运行层面。它不调用在线 LLM 或真实平台 API，只从本地 ledger 派生 account memory，保留 prompt hash 和 draft text hash，不保存 prompt 明文；recent trace summaries 受数量限制，`lifetimeStats` 只保存全量紧凑聚合计数和 top topics；reflection 写入 `ai_runs`、`runtime_snapshot` evidence 和 `account_memory_reflected` audit event，供后续 AI 选题和发帖前读取。

### Relations
* `splits-from` `20260622A`：从第一主任务中拆出的 account memory/reflection baseline 子任务。
* `follows` `20260622A.013`：建立在单次离线 orchestration run 已可完整写入 ledger 之后。

## Task ID `20260622A.015`

### Summary
* 路径：`doc/tasks/snapshots/20260622A post-foundry foundation/20260622A.015 LLM draft adapter boundary/`
* 状态：已收口，Close Gate 通过。
* 范围：冻结并实现离线 LLM draft adapter 边界，把 `.009` draft input package、`.014` account memory 和 offline fixture provider 接到同一个可审计的 draft generation contract。
* 相关 active spec / guide：`04_review/01_close_gate_review.md`。
* 摘要：`.015` 不调用在线 LLM、不访问 X/TwitterAPI.io/Telegram。它构造 prompt-safe `llm_draft_request_v1`，只传 prompt hash 和紧凑 memory；provider 输出必须通过 `.009` draft parser；adapter run 写入 `ai_runs` 和 `llm_draft_generated` audit event，ledger 中只保存 sanitized request、draft/post text hash 和 usage 摘要。

### Relations
* `splits-from` `20260622A`：从第一主任务中拆出的 LLM draft adapter boundary 子任务。
* `follows` `20260622A.014`：建立在账号级 memory/reflection baseline 已可为下一次草稿生成提供上下文之后。

## Task ID `20260622A.016`

### Summary
* 路径：`doc/tasks/snapshots/20260622A post-foundry foundation/20260622A.016 online operation runner baseline/`
* 状态：已收口，Close Gate 通过。
* 范围：冻结并实现在线运营 runner baseline，提供 `prod-online-run-once` 和 `prod-online-run-loop` 两个手动入口，二者复用同一个 once runner，并用账号级 lock 保证同账号不并发跑完整流程。
* 相关 active spec / guide：`04_review/01_close_gate_review.md`。
* 摘要：`.016` 先落架构底座，不接真实 provider。一次性入口和循环入口共享 `runOnlineOperationOnce`；循环入口支持 interval seconds、jitter seconds 和 UTC sleep window；锁默认位于 `data/locks/operation.<account_key>.lock`，使用完整 JSON hard-link 原子创建，支持 stale/corrupt cleanup、heartbeat sidecar refresh 和 300 秒最小循环间隔。真实 LLM、source collection、Telegram 和 X executor 后续接入时必须复用这个底座。

### Relations
* `splits-from` `20260622A`：从第一主任务中拆出的 online operation runner baseline 子任务。
* `follows` `20260622A.015`：建立在 LLM draft adapter boundary 已固定 request/result/ledger sanitize 之后。

## Task ID `20260622A.017`

### Summary
* 路径：`doc/tasks/snapshots/20260622A post-foundry foundation/20260622A.017 run-once operation executor baseline/`
* 状态：已收口，Close Gate 通过。
* 范围：冻结并实现 run-once operation executor baseline，保留生产 `prod-online-run-once` / `prod-online-run-loop` 的 not_wired 纯净语义，并把离线 fixture executor 隔离到 focused test 和显式 offline debug 入口。
* 相关 active spec / guide：`04_review/01_close_gate_review.md`。
* 摘要：`.017` 明确生产入口和离线 fixture 不混用：`prod-online-run-once` 与 `prod-online-run-loop` 在真实生产 executor 接入前只返回 `production operation executor is not wired yet`，不打开默认 runtime DB、不写假 ledger；离线 fixture executor 通过 `test-offline-run-once-fixture` 和 `debug-offline-run-once-fixture --db-file ...` 验证账号配置快照、prompt hash、source/recent context、draft gate、policy 和 final action ledger。

### Relations
* `splits-from` `20260622A`：从第一主任务中拆出的 run-once operation executor baseline 子任务。
* `follows` `20260622A.016`：建立在 once/loop runner、账号级锁和循环调度已落地之后。

## Task ID `20260622A.018`

### Summary
* 路径：`doc/tasks/snapshots/20260622A post-foundry foundation/20260622A.018 production source collection v0/`
* 状态：已收口，Close Gate 通过。
* 范围：冻结并实现 production source collection v0，把账号配置里的 public X search keywords 接到 TwitterAPI.io source adapter，生成账号隔离 source materials，并写入 API audit、AI run、evidence refs 和 audit event。
* 相关 active spec / guide：`04_review/01_close_gate_review.md`。
* 摘要：`.018` 只接第三方公开数据读取，不接 X official 发帖或在线 LLM。`collectAccountPublicXSourceBatch` 按账号 enabled、public_x enabled 和 monthly_request_cap 做边界控制，成功收集时每个 query 都有 `api_call_audit`，整体收集有 compact `ai_run` 和 source evidence refs；`debug-online-source-collection` 默认 dry-run，只有显式 `--collect --config-file config/accounts.local.json` 才访问 TwitterAPI.io 并写 runtime ledger；example config 只允许 dry-run/文档示例。

### Relations
* `splits-from` `20260622A`：从第一主任务中拆出的 production source collection v0 子任务。
* `follows` `20260622A.017`：建立在生产入口和离线 fixture executor 已分离、executor 边界已固定之后。

## Task ID `20260622A.019`

### Summary
* 路径：`doc/tasks/snapshots/20260622A post-foundry foundation/20260622A.019 production run-once source collection integration/`
* 状态：已收口，Close Gate 通过。
* 范围：把 `.018` production source collection 接入 `.016` once/loop runner 和 `.017` production entrypoint，让 `prod-online-run-once` / `prod-online-run-loop` 能执行真实公开 X source collection 并写 runtime ledger。
* 相关 active spec / guide：`02_spec/01_production_run_once_source_collection_spec.md`。
* 摘要：`.019` 只接第三方公开数据读取，不接在线 LLM、Telegram 或 X official 发帖。生产入口必须显式传真实 `--config-file`，拒绝 example config；once 和 loop 共用同一个 production source collection executor，并继续使用账号级 lock 避免同账号并发运行。

### Relations
* `splits-from` `20260622A`：从第一主任务中拆出的 production run-once source collection integration 子任务。
* `follows` `20260622A.018`：建立在 production source collection v0 已能按账号写 source ledger 之后。
