# Task Meta

* Task ID: `20260622A.012`
* Title: `topic radar and selection baseline`
* Status: `已收口，Close Gate 通过`
* Parent task: `20260622A`
* Follows: `20260622A.011`
* Workspace change: `ypoqowqv`

## Goal

把 `.011` 产出的公开资料 / 高赞帖 / 人工笔记，在进入 `.010` source context 和 `.009` draft pipeline 前，先做账号级选题雷达：按账号主题过滤、聚合同类素材、避开近期重复主题、输出可审计的 `selectedTopic`。

## Non-goals

* 不调用真实 TwitterAPI.io。
* 不抓新闻或网页。
* 不访问 `x.com`。
* 不调用 X official API。
* 不调用在线 LLM。
* 不执行真实发帖或 Telegram 发送。
* 不实现 scheduler、crawler、prompt 自动沉淀或在线 debug CLI。
