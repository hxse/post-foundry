# Problem Context

`.019` 让生产 runner 能真实收集公开 X source materials，但采集结果还没有进入“AI 运营判断”。为了向自动化运营闭环前进，生产 run 需要把本轮 source materials 转成可审计的 topic radar 和 source context。

`.020` 仍然不接 LLM 和发帖，目标是在同一个 trace 下沉淀：采集了什么、选题为什么被选中、最终给后续草稿生成的 source context 是什么。
